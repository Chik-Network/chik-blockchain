---
description: Chik test harness selection and routing to domain-specific guides
alwaysApply: true
---

# Chik Test Guide

When writing or modifying tests under `chik/_tests/`, follow this sequence:

## 1. Route to the right guide

Identify the area from the user-provided path or intent, then read the matching guide **before any code search**:

- `chik/_tests/blockchain/**` or consensus/reorg/overflow intent -> `testing-guide-blockchain.mdc`
- `chik/_tests/core/full_node/**` or sync/full-node intent -> `testing-guide-full-node.mdc`
- `chik/_tests/core/mempool/**` or mempool intent -> `testing-guide-mempool.mdc`
- `chik/_tests/core/data_layer/**` or data-layer intent -> `testing-guide-data-layer.mdc`
- `chik/_tests/core/server/**` or server/network intent -> `testing-guide-server.mdc`

After reading, state: `Guide loaded: <guide-file> (area: <area>)`.
If area is ambiguous, ask one clarifying question instead of broad repository search.

## 2. Pick the right harness

1. **Pure logic / parsing / utility** — no simulator, standard pytest.
2. **Chain state / farming / wallets / mempool / reorg** — simulator fixtures from `chik/_tests/conftest.py`:
   - `simulator_and_wallet`, `one_node`, `two_nodes`, `one_node_one_block`
   - `wallet_environments` for wallet-heavy tests
3. **Service wiring (farmer/harvester/solver/timelord)** — `setup_*` in `chik/_tests/util/setup_nodes.py`.
4. **Lightweight KLVM spend sim** — `sim_and_client()` from `chik/_tests/util/spend_sim.py`.

Key shared utilities: `chik/_tests/conftest.py`, `chik/simulator/block_tools.py`, `chik/_tests/util/time_out_assert.py`, `chik/_tests/environments/wallet.py`.

For detailed block/tx/assertion patterns, see `testing-guide-patterns.mdc`.
