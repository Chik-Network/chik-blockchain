---
description: Chik test harness routing — pick the right guide and harness before writing tests
globs:
  - chik/_tests/**
---

# Chik Test Guide

When writing or modifying tests under `chik/_tests/`, read the matching on-demand
guide before any code search.

## Route to the right guide

| Path or intent | Read first |
|----------------|------------|
| `chik/_tests/blockchain/**` or consensus/reorg/overflow | `.cursor/context/testing/blockchain.md` |
| `chik/_tests/core/full_node/**` or sync/full-node | `.cursor/context/testing/full-node.md` |
| `chik/_tests/core/mempool/**` or mempool | `.cursor/context/testing/mempool.md` |
| `chik/_tests/core/data_layer/**` or data-layer | `.cursor/context/testing/data-layer.md` |
| `chik/_tests/core/server/**` or server/network | `.cursor/context/testing/server.md` |
| `chik/_tests/core/**` cross-subsystem or service wiring | `.cursor/context/testing/architecture.md` |
| `chik/_tests/wallet/**` | `.cursor/context/testing/wallet.md` |
| `chik/_tests/clvk/**` or SpendSim | `.cursor/context/testing/clvk.md` |
| `chik/_tests/cmds/**` or CLI | `.cursor/context/testing/cmds.md` |
| `chik/_tests/util/**` or shared harness | `.cursor/context/testing/infrastructure.md` |
| Any test area or CI partitioning | `.cursor/context/testing/architecture.md` |
| Block/tx/assertion patterns for all tests | `.cursor/context/testing/patterns.md` |

After reading, state: `Guide loaded: <guide-file> (area: <area>)`.

## Pick the right harness

1. **Pure logic / parsing / utility** — no simulator, standard pytest.
2. **Chain state / farming / wallets / mempool / reorg** — simulator fixtures from
   `chik/_tests/conftest.py`: `simulator_and_wallet`, `one_node`, `two_nodes`,
   `one_node_one_block`; `wallet_environments` for wallet-heavy tests.
3. **Service wiring (farmer/harvester/solver/timelord)** — `setup_*` in
   `chik/_tests/util/setup_nodes.py`.
4. **Lightweight CLVK spend sim** — `sim_and_client()` from
   `chik/_tests/util/spend_sim.py`.

Key shared utilities: `chik/_tests/conftest.py`,
`chik/simulator/block_tools.py`, `chik/_tests/util/time_out_assert.py`,
`chik/_tests/environments/wallet.py`.
